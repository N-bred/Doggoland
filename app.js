window.addEventListener('load',()=>{const $=el=>document.querySelector(el);const $a=el=>document.querySelectorAll(el);const header=$('.header');const products=$('.products');const about=$('.about');const homeLink=Array.from($a('.home-link'));const productsLink=Array.from($a('.products-link'));const aboutLink=Array.from($a('.about-link'));const learnMoreLink=$('.nav__main__link');const moveToPosition=(e,pos)=>{e.preventDefault();scrollTo({top:pos,left:0,behavior:'smooth'})};const doForEach=(array,elementToMove)=>{array.forEach(el=>{el.addEventListener('click',e=>moveToPosition(e,elementToMove.offsetTop))})};learnMoreLink.addEventListener('click',e=>moveToPosition(e,products.offsetTop));doForEach(homeLink,header);doForEach(productsLink,products);doForEach(aboutLink,about);const copy=$('#copy');const date=new Date();copy.innerText=date.getFullYear()})